{
  "name": "build-helper",
  "version": "1.0.0",
  "description": "A helper for build package in monorepo",
  "scripts": {
    "prune": "ts-node --transpile-only src/prune.ts",
    "clean": "ts-node --transpile-only src/clean.ts"
  },
  "private": true,
  "dependencies": {
    "fs-extra": "^8.1.0",
    "lerna": "^3.20.2"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.0.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  }
}
